PORT=5001
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
NODE_ENV=development

# Must match INTERNAL_API_KEY on the API Gateway
API_KEY=supersecretkey_change_me_in_production

# JWT_SECRET is no longer needed — JWT is verified at the gateway level.
# Remove or leave for reference only.
# JWT_SECRET=supersecret_change_me_in_production
